import Index from './components/basic/Index'

export default {
    mode: 'history',
    routes: [
        {
            path: '/',
            name: 'index',
            component: Index,
        }
    ]
}